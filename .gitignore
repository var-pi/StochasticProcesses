*.jl.cov
*.jl.*.cov
*.jl.mem
/docs/build/
.DS_Store
# Manifest.toml is intentionally committed (reproducibility) — do NOT ignore it.
# experiments/**/figures/ are committed artifacts — do NOT ignore them.

# LaTeX build artifacts (compiled .pdf and source .tex are tracked)
*.aux
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.blg
*.nav
*.snm
*.vrb
